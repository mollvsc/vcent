source "https://rubygems.org"

# Matches the Jekyll/plugin versions GitHub Pages builds with, so what you
# see in local preview matches what ships. See README for local setup notes.
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3+; harmless on older Rubies.
gem "webrick"
